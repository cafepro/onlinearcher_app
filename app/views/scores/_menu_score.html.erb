<!-- Collect the nav links, forms, and other content for toggling -->
<div class="menu_score">
    <ul class="menu">


        <li>
        	<%= link_to t('score.edit').html_safe, edit_score_path(@score), remote: true %>
        </li>

        <li>
        	<%= link_to t('main.destroy').html_safe, @score, method: :delete, remote: true, data: { confirm: t('main.confirm_delete') } %>
        </li>

    </ul>
</div>
<%= form_for @score, remote: true do |f| %>
  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :score_type_id, options_from_collection_for_select(ScoreType.published, :id, :name, @score.score_type_id) %>
  </div>
  <div class="field">
    <%= f.text_field :name, placeholder: t('score.session_name') %>
  </div>

  <div class="field">
    <%= f.submit t('main.start'), class: 'btn btn-primary btn-block' %>
  </div>

<% end %>
